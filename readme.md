# SICP

Example code from reading through SICP

## Running Programs

The included `.scm` files can be run as follows:

```sh
scheme --load {filename}
```

Provided that `mit-scheme` has been installed as `scheme`.
